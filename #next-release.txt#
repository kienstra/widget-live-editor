WLE

Padding-top of widgets : heading has much more padding than image, everything should have same

new widget : remove query var, maybe nonce 
Remove title of widget, or assign it automatically. It serves no purpose.
Fix bug where if user types no heading or body, they don't appear on front page
After creating widget, "Save changes" prompt before "Edit content" 
options : center
	 remove "edit" anchor or integrate with ddw
apply_filters or option to remove "move_widget"
in customize-register: use get_pages()

BWS:
minor:
	x new in wrong repo: js/bws-change-markup.js : two changes
	x on customizer-controls-enqueue-script : load the script for pages, meta , etc...
	x recent comments changed ( in both repos ? ) 
	x tag cloud done and copied in root repo
	filter wp_nav_menu to output list-group with accordion drop-down
	bug in archives : unexpectied </li> after <a>

BSG : know exclude gallery images from post carousel
			have been editing in /var/www/
			no controls if only one image on page
			Make a pop-up of all image on page, except for gallery images. Each gallery gets its own.
				php : exclude gallery items in post, same in js, maybe .not( '.gallery-item'	)	
	php fine, just add a conditional to enqueue the js; if page has images
	javascript needs to be refactored
						just find the ordinal of the image on the page, excluding gallery images, and activate that in the gallery
		 need a marker for top of post , to exclude sidebar or header images
		 maybe add class to post title ? 
		 
				bug : if post is split into pages, selects wrong images after first page
	
THEME OPTIONS : HEADER AND FOOTER MARKUP , allow comments on everything, or only pages

			 

Paths:
WLE : ~/wp/eight/
bws : ~/wp/eight/	bsg_res			( used to be: widget_wle )
bsg : ~/wp/eight/	bsg-1-0-1
bwr : /var/www/	 
bpw : /var/www/
brv : /var/www/

top banner :
<form class="navbar-form navbar-left opt-in-fields" role="search">
			<div class="form-group">
				<input type="text" class="form-control" placeholder="Name">
				<input type="text" class="form-control" placeholder="Email">
			</div>
			<button type="submit" class="btn btn-primary btn-med">Free updates</button>
		 </form>

bottom navbar :
<div class="navbar-form navbar-left">
	[mc4wp_form]
</div>

		 
classes of header images: thumbnail wp-post-image

theme review: get_stylesheet_uri header.php? 
